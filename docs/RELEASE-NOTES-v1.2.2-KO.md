# Body Change NG v1.2.2

## v1.2.1 대비 변경

- Breasts/breasts, Waist/waist, HipBone/Hipbone처럼 대소문자만 다른 슬라이더의 충돌을 수정했습니다. XML의 small/big 값을 하나의 모프로 병합하며 처음 표기된 이름은 유지합니다.
- 미리보기와 가슴·유두 의상 보정에서 대소문자가 다른 이름을 잘못 합성하던 문제를 수정했습니다. 미리보기 취소 시 확정 상태로 복원하는 동작은 유지합니다.
- UNP 역방향 기본 슬라이더에도 같은 이름 비교를 적용했습니다. 음수·100% 초과 값을 유지하며 원본 XML과 코세이브 형식은 바꾸지 않았습니다.
- 모프 보존을 켠 상태의 OBody/OClothe 키 정리, 후타스킨 즐겨찾기, 성별별 NPC 배포 등 1.2.1의 기존 기능은 모두 유지합니다.
- 최초 공개 1.2.1 이후 추가했던 체크박스 미리보기 액터의 커스텀 팔로워·노인 자동 선택 제외도 포함합니다. 일반 액터 직접 선택과 실제 배포 규칙은 바꾸지 않습니다.

## 업데이트 방법

1. 게임을 종료하고 세이브와 같은 이름의 `.skse` 코세이브, 설정·사용자 규칙·에셋 팩을 백업합니다.
2. BCNG DLL을 v1.2.2로 교체하고 활성 DLL은 하나만 유지합니다. `Data\SKSE\Plugins\BodyChangeNG\settings.json`, 사용자 배포 규칙과 기존 팩은 보존합니다. 규칙을 배포본의 빈 시작 파일로 덮어쓰지 마세요.
3. 기존 세이브를 로드해 체형을 확인합니다. 이번 업데이트만을 위해 새 게임이나 세이브 정리를 할 필요는 없습니다. OBody 모프를 명시적으로 정리하려면 프리셋 또는 기본 바디를 확정합니다.

실행 중인 OBody의 자체 재배포를 끄는 기능은 아닙니다. 같은 액터의 체형을 두 시스템이 계속 재지정하지 않도록 설정하세요. 메시의 Zeroed Sliders·Build Morphs 준비는 여전히 필요하며, 키 정리로 메시 자체에 구워진 체형을 바꿀 수는 없습니다.

## 검증 — 2026-09-13

- SE/AE 전용 Release DLL 빌드 완료. 파일 버전 `1.2.2.0`.
- DLL SHA-256: `BBEED2C787B8C275A9533282CC7BAFF6FCF71AC0E9A87A986B47BEF639D3B20B`.
- 가까운 여성/남성/등록 후타 NPC와 플레이어 복귀 정책 테스트, 배포 성별 분리·바디 타입 필터·도구 행 변경 직후 이전 목록 처리 방지 연결 검사 통과.
- 자동 테스트 24종 통과. 모프 키 테스트 4,509개 확인: 보존 ON/OFF, 정확한 두 키 삭제, 대소문자가 다른 이름의 반복 미리보기·취소·확정, OBody에만 있는 슬라이더, 기본 복원, 다른 키 보존 등. 프리셋 테스트에서도 XML 양 끝값의 대소문자·순서 변경, +120/-150 값, 지정 의상 보정과 UNP 반전을 확인했습니다.
- 주기 검사·스레드·새 세이브 마이그레이션을 추가하지 않았고 1.2.1의 교체 정책을 유지합니다. 자동 테스트가 실제 게임의 성능·누수 측정을 대신하지는 않습니다.
- 이번 추가 수정 DLL을 TuLED·TAKEALOOK MO2에 반영하고 해시 일치를 확인했습니다. 설정·규칙 JSON과 에셋 팩을 보존하고 교체 전 파일은 백업했습니다. 인게임 검증은 아직입니다.

[전체 소개와 사용 안내](NEXUS-DESCRIPTION-v1.2.2-KO.html) · [한국어 체인지로그](NEXUS-CHANGELOG-v1.2.2-KO.html)
