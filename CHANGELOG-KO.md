# 변경 내역

## 1.3.3

- `BodyChangeNGdistribution.json`에서 `//` 한 줄 주석과 `/* ... */` 여러 줄 주석을 읽도록 했습니다. 기존 주석 없는 schema 7 파일도 그대로 지원합니다. 다른 JSON 입력, 규칙 판정, 지원 게임·RaceMenu 인터페이스와 고정 의존성은 변경하지 않았습니다.
- 빈 기본 파일 대신 필드 19개·대상 조건 11종·비활성 예제 19개를 설명하는 한영 주석 템플릿을 제공합니다. 확인된 기본 게임 조건 값 39개, 커스텀 레코드 찾기, 여러 후보 입력 예제를 포함합니다. 바디프리셋·스킨·후타스킨·오버레이 색상, 플러그인과 로컬 BaseID, 프리셋 식별자, 우선순위, 호환 조건, 설치와 다른 게임 구성으로의 이동을 설명합니다. 기본 활성 규칙은 여전히 0개입니다.
- 프리셋 ID와 등록된 오버레이 ID를 출력하는 선택적 읽기 전용 PowerShell 도구와 별도 한영 작성 안내를 추가했습니다. 도구는 에셋·설정·세이브를 수정하지 않습니다.
- 주석 파일은 1.3.3 이상 DLL이 필요합니다. 인게임 규칙 저장 시 실제 데이터는 맨 위에, 기존 주석 가이드·비활성 예시·사용자 메모는 아래에 보존합니다. 반복 저장해도 주석이 중복되거나 예시가 활성화되지 않습니다. 주석 없는 파일에 가이드를 자동 삽입하지 않으며 게임 중 반복 검사도 추가하지 않았습니다.
- 한글 소개글·체인지로그 HTML은 브라우저의 밝은/어두운 테마를 따릅니다. 본문 흰색 고정 대신 시스템 글자색을 사용하고 제목·링크·코드·표도 테마에 맞는 대비로 표시합니다.
- 검증: Release DLL 빌드, 오프라인 회귀 실행 파일 35개 전체와 작성 도구 검사 통과. 실제 읽기 함수, 모든 필드·대상 조건, 예제 비활성·활성화, 문자열 속 주석 기호, 잘못된 JSON, 프리셋 ID·오버레이 해시, 배포 저장 88개 조합을 확인했습니다. 읽기·저장 실패 시 원본을 보존하고, 성공 시 실제 데이터 아래 주석을 유지하며 25회 반복 저장도 동일한 바이트를 유지합니다. 이번 변경의 인게임 테스트는 수행하지 않았습니다.

## 1.3.2

- 바디 계열을 판별하지 못한 독립형 팔로워도 스킨을 직접 선택할 수 있도록 수정했습니다. 성별·종족에 맞는 팩과 계열 미확인 안내를 표시하며, 배포 규칙 없이 한 번 클릭으로 미리보기·더블클릭으로 적용합니다. 확인된 계열 간 비호환 제한은 유지합니다.
- 자동 NPC 배포의 기존 호환성 검사는 유지합니다. 저장된 수동 선택 재적용, 미리보기 취소, 플레이어 RaceMenu 복원에서도 직접 선택 의도를 보존합니다. 몸·손·발·얼굴 적용 경로를 유지하며, 계열 미확인 상태에서 CBBE/UNP 성기·항문 보조 아틀라스는 추측하여 적용하지 않습니다.
- BCNG 내부 스킨이 활성화된 동안 원래의 계열 미확인 상태도 보존하여 첫 선택 이후 캐시 DDS 이름으로 액터가 재분류되지 않도록 했습니다. 새 폴링·캐시 형식·세이브 스키마·의존성 변경은 없습니다.
- NPC 얼굴 대상은 HeadPart 식별 이름과 실제 메시 이름이 반드시 같다는 전제 대신 실제 FaceGen 영역에서 찾습니다. 정확한 이름을 우선하고, 명확하게 하나인 피부 재질 대상만 대안으로 사용하며 얼굴 밖의 동명 객체는 거부합니다. 복원할 이전 얼굴 상태가 없는 몸 전용 스킨은 얼굴 대상 준비를 요구하지 않습니다.
- 검증: Release 빌드, 오프라인 회귀 실행 파일 35개, 보조 검사 19개 통과. 새 인게임 테스트는 수행하지 않았으며 제보자의 특정 환경에서 해결됐는지는 아직 확인되지 않았습니다. [검증 기록](docs/RELEASE-VERIFICATION-v1.3.2-KO.md).

## 1.3.1

- 선택적으로 사용하는 오프라인 캐시 정리 도구를 추가했습니다. MO2 Overwrite를 먼저 변경 없이 검사하고, 명시적으로 확인하면 내용과 수정 시각이 같은 DDS 복사본을 하드링크로 합칩니다. 예전 세이브와 1.2.7 캐시가 사용하는 DDS 경로도 유지합니다.
- 선택한 캐시 밖에 연결된 하드링크, 읽기 전용·잠긴 파일, 링크/정션과 알 수 없는 구조는 제외합니다. 오래됐거나 현재 선택되지 않았다는 이유로 고유 DDS를 삭제하지 않습니다. 절약량은 실제 중복 상태에 따라 다르며 폴더 표시 용량은 그대로일 수 있습니다.
- 게임에 전달되지 않는 것으로 확인된 준비/링크 임시 파일만 게임 종료 후 정리합니다. Skyrim 실행 중에는 도구가 작업을 거부하며 원본 스킨팩·세이브·배포 규칙은 수정하지 않습니다.
- 런타임 DDS를 임시 경로에서 완성한 뒤 원자적으로 교체합니다. 준비 실패 시 기존 정상 캐시를 유지하고 자신이 만든 임시 파일을 정리하며 읽기 전용 원본 속성은 보존합니다. 몸·얼굴·성기·동반 맵은 기존 캐시 식별 방식을 유지합니다.
- 1.3.0의 복원 수정, 기존 외형 기능·지원 런타임·RaceMenu 인터페이스·고정 의존성을 유지합니다. 정리 도구는 게임 중 실행하지 않으며 매 프레임 검사를 추가하지 않았습니다.
- 검증: Release 빌드, 회귀 실행 파일 35개, 보조 검사 19개, 캐시 AddressSanitizer 검사 통과. 바이트/경로 보존, 외부 하드링크 격리, 잠긴 파일 실패/재시도와 임시 파일 정리를 확인했습니다. 새 인게임 테스트나 설치 캐시의 실제 정리는 하지 않았습니다. [검증 기록](docs/RELEASE-VERIFICATION-v1.3.1-KO.md)을 참고하세요.


## 1.3.0

- 여러 저장 오버레이를 복원할 때 실제 노드 쓰기가 다음 틱에 실행되더라도 서로 다른 슬롯을 예약합니다. 기존 고유 슬롯을 먼저 확보한 뒤 미배정·범위 밖·중복 슬롯을 배정하며 색상을 유지하고 다른 모드가 점유한 슬롯은 덮어쓰지 않습니다.
- 기본 바디 자동 검증에서 바디 자체와 독립적인 의상 보정을 구분합니다. 유효한 가슴·유두 보정을 지우지 않으며 실제 바디 정리가 필요할 때만 이전 보정 서명을 무효화하고 정상 의상 정책으로 재평가합니다. 명시적 초기화·모드 삭제 준비의 소유 값 정리는 유지합니다.
- RaceMenu가 열려 있으면 플레이어 스킨 미리보기 취소 복원을 보류하고 닫힌 뒤 처리합니다. 액터 언로드로 취소 복원이 사라지지 않게 유지하면서 텍스처 준비 후속 작업에도 같은 대기 조건을 전달하고 이전 세션의 작업은 거부합니다. 새 선택이 이전 복원을 무효화하는 정책은 유지합니다.
- RaceMenu에서 플레이어를 편집하는 동안 바디 미리보기 취소·남은 미리보기 키 정리를 보류하고 닫힌 뒤 복원합니다. 언로드 시 정리와 새 활성 미리보기 보호는 유지합니다.
- RaceMenu 진입으로 오버레이 미리보기 취소가 사라지지 않도록 수정했습니다. 부위별 최신 선택과 액터 실행 순서·지연 쓰기를 유지하여 오래된 취소가 새 미리보기·색상 편집·확정·초기화를 덮어쓰지 않게 했습니다.
- 로드 또는 RaceMenu 종료 시 플레이어의 저장된 기본 바디를 복원한 뒤 의상 보정도 재평가합니다. 비활성화·누드·SFS·지정/자동 보정의 기존 정책을 따르며 명시적 초기화·모드 삭제 준비는 보정을 다시 적용하지 않습니다.
- 배포 규칙·세이브 형식·SE/AE 지원 범위·RaceMenu 인터페이스·고정 의존성은 유지합니다. 새 영구 캐시·엔진 폼 강제 해제·전체 액터 상시 스캔은 추가하지 않았습니다.
- 검증: Release 빌드, 회귀 실행 파일 34개, 보조 검사 19개 통과. 실제 제품 함수 기반 전환 검사, 혼합 큐 동작 1,000,000회, AddressSanitizer 검사를 수행했습니다. 8개 그룹의 호스트 할당 검사 3,029,300회에서 잔여 증가가 없었으며 부위별 복원과 보류 큐 2,000,000회 반복을 포함합니다. 오프라인 검사로 게임 전체 누수나 렌더링 무결성을 보장하는 것은 아닙니다. [검증 기록](docs/RELEASE-VERIFICATION-v1.3.0-KO.md)을 참고하세요.

## 1.2.9

- NPC가 언로드된 뒤 스킨 미리보기를 취소해도 이미 반영된 임시 스킨을 복원합니다. 새로운 미로드 선택과 취소 복원을 구분하고, 3D를 강제로 로드하지 않습니다. 세션·새 선택 무효화 검사는 유지하며 언로드 뒤에는 확정 얼굴 상태를 다시 확인합니다.
- 확정된 BCNG 기록이 없는 NPC도 언로드된 단일·체크박스 누적 오버레이 미리보기를 정리합니다. 늦게 실행된 정리가 새 미리보기·확정 오버레이/색상·다른 텍스처를 지우지 않도록 처리합니다.
- 기본 스킨 복원은 내부 바디와 얼굴 정리가 모두 성공했을 때만 완료로 기록합니다. 일부 실패 시 재시도에 필요한 복원 정보를 유지하고, 실패한 복구를 성공으로 표시하지 않습니다.
- 일반 적용·호환성 조건·배포 규칙·세이브 형식·의존성·기존 SE/AE 지원 범위는 유지합니다. 새 영구 캐시·상시 검사·엔진 폼 강제 해제는 추가하지 않았습니다.
- 검증: Release 빌드, 회귀 실행 파일 32개, 보조 검사 19개, 12개 런타임의 메타데이터 주소 검사 통과. 제품 함수의 부분 실패/재시도와 미리보기 언로드 10,000회, 큐의 언로드·새 선택·세션 초기화 경계를 검사했습니다. 오프라인 검사이며 전체 게임 누수·렌더링 무결성을 보장하는 것은 아닙니다. [검증 기록](docs/RELEASE-VERIFICATION-v1.2.9-KO.md)을 참고하세요.

## 1.2.8

- 일반 전체 초기화에서도 공유 NPC 베이스의 각 액터 얼굴을 개별 복원하고, 같은 Default 요청의 몸 갱신은 중복하지 않습니다. 새 스킨 선택이 끼어들면 이전 초기화 요청은 무효화합니다.
- NPC 언로드와 미리보기 취소가 겹쳐도 임시 바디 모프 키를 정리합니다. 현재 진행 중인 미리보기·확정 모프·다른 모드 모프는 유지하고 3D를 강제로 로드하지 않습니다.
- 오버레이 색상 팝업의 Esc 및 매핑된 Cancel 입력은 팝업만 닫도록 수정했습니다.
- 삭제 준비 확인창에서도 부모 창보다 안쪽 팝업이 먼저 Cancel을 처리하고, 다른 팝업처럼 위치를 기억하도록 보완했습니다.
- 바디스킨·후타스킨·틴트 목록을 불변 카탈로그 공유 및 화면 내 행 처리로 변경했습니다. 스킨 텍스처 수와 틴트팩 묶음을 재사용하며 키보드 포커스·체크박스 선택은 유지합니다.
- 액터 선택 기록을 기존 ASTR 형식의 여러 레코드로 나눠 저장하여 16,384명 초과 시 잘라내지 않습니다. 사용하지 않은 배포 결과로 빈 기록을 만들지 않고, 실제 선택·Default·복원 정보가 없는 일시적 보정 캐시는 언로드 시 정리합니다.
- VM이 응답 없이 콜백을 해제하거나 응답 전달 작업이 폐기되면 얼굴 배치를 실패·복원 경로로 정리합니다. 롤백과 요청 세대 확인은 유지하며 VM이 아직 보관하는 콜백을 임의 시간 제한으로 취소하지 않습니다. 지원되는 경우 이전 얼굴 노드 정리의 읽기·제거에도 기존 네이티브 경로를 사용합니다.
- 모델의 대체 텍스처 배열을 엔진의 개수 헤더 규약에 맞게 할당·해제하도록 수정했습니다. 할당 실패 시 기존 배열은 유지하고 공개되지 않은 텍스처의 생성 참조를 정리합니다. SE 1.5.97과 AE 1.6.1170에서 관련 엔진 코드를 읽어 확인했으며 실행 중 게임 메모리는 수정하지 않았습니다.
- 몸·원거리 스킨 그래프 생성 실패 시 검증된 비공개 ARMO/ARMA/FLST 복제물과 TXST 생성 참조를 정리합니다. TNG가 보관할 수 있는 완성된 장기 유지 그래프는 파괴하지 않습니다. 그래프 교체 전 private 텍스처 채널 하나라도 복원이 실패하면 기존 복원 근거를 유지합니다. 콜백 누락·롤백 경로·텍스처 실패·배열 교체·몸/원거리 그래프 생성 실패 검사를 확대했으며 실제 누수량 측정이나 제보된 TNG 텍스처 문제의 인게임 재현은 아닙니다.
- 기본값 복원과 세션 초기화 시 액터의 포인터뿐 아니라 BCNG가 재사용하는 내부 스킨의 DDS 경로도 원복합니다. TNG처럼 그 내부 아머 애드온을 참조하는 조합을 보관하는 제공자에 이전 BCNG 선택이 남지 않도록 보완하며, 제공자의 원본 폼은 수정하지 않습니다.
- 새 텍스처 적용을 준비할 때 이전에 확인했던 캐시 파일도 다시 확인하고, 삭제된 경우 원본이 남아 있으면 재생성합니다. 성기 텍스처 훅이나 매 프레임 렌더링에는 파일 검사를 추가하지 않았습니다.
- 설정에 **모드 삭제 준비**를 추가했습니다. 확인 후 자동 배포·보정을 지속적으로 중단하고 BCNG 소유 값만 정리하여 완료/미완료를 구분합니다. 배포 규칙 파일과 다른 모드의 모프 키는 보존합니다. 미로드 액터의 저장된 얼굴 오버라이드는 기존 NiOverride 공개 경로로 복원하고, 같은 바디 베이스를 공유해도 각 액터의 얼굴을 별도로 정리합니다. 확인에 실패하면 복원 기록을 유지합니다. 완료 후 새 슬롯에 저장하고 게임을 완전히 종료한 뒤 삭제해야 하며, 이미 손상된 세이브 전체를 복구하는 기능은 아닙니다.
- 기존 SE/AE 12개 런타임과 RaceMenu BodyMorph v4/v5·Override/Overlay v1/v2 대응을 유지합니다. 1.7.x·VR 지원은 추가하지 않습니다. TNG/SOS 크기 변경 조사에서 TNG 메뉴가 크기 선택 전에 애드온을 재선택하고 3D를 재생성할 수 있는 경로를 확인했지만, 제보된 반보라색 렌더링을 인게임에서 재현한 것은 아닙니다.

- 검증은 변경점뿐 아니라 기존 전체 기능군을 포함합니다. Release 빌드, 회귀 실행 파일 31개, 오프라인 보조 검사 19개, 12개 런타임의 배포 메타데이터 주소 검사와 별도 할당·목록 성능 검사를 수행했습니다. 실제 배포 저장 함수의 조건·성별·기능 88개 조합과 파일 교체 실패·재시도 검사도 통과했습니다. [전체 기능 검증표](docs/RELEASE-VERIFICATION-v1.2.8-KO.md)를 참고하세요. 새 인게임 테스트는 수행하지 않았으며 버그·게임 전체 누수가 전혀 없다는 보장은 아닙니다.

## 1.2.7

- 배포 조건의 팩션·종족·클래스·키워드 목록에서 실제 런타임 타입과 현재 폼 등록 상태를 확인한 후 정보를 읽습니다. 이름은 검증된 구성요소에서 위험한 가상 함수 호출 없이 읽으며, Editor ID 제공 훅은 유지합니다. 이름 부분만 사용할 수 없으면 기존 ID 표시로 항목을 유지하고 특정 팩션을 이름·ID로 제외하지 않습니다.

- 본체 창 닫기·재열기·게임 세션 전환 때 배포 체크박스 모드·성별·선택 후보·미리보기 갱신 플래그를 함께 초기화합니다. 조건 팝업으로 넘어갈 때는 후보를 보존하여 추가 규칙에 사용할 수 있도록 유지합니다.
- 종족·팩션·키워드·클래스·플러그인 조건 목록은 UI 그리기 중 엔진 배열을 순회하지 않고 게임 작업에서 수집합니다. 문자열·ID를 소유하는 읽기 전용 결과만 전달하고 닫기·로드 뒤 도착한 결과는 폐기합니다. 매 프레임 재수집이나 실패 시 무한 재시도는 하지 않으며, 저장된 규칙·전체 NPC·이름 조건은 목록 준비와 별개로 유지합니다.
- 직렬화 Revert와 새 게임·로드 완료 경계에도 UI 정리를 연결했습니다. 이전 액터·카메라 참조를 새 게임에 복원하지 않고 정리하며 이전 세션의 대기 카메라 갱신도 무효화합니다.
- 바디 미리보기 복원·바디 적용·네이티브 스킨 변경 직전에 취소 및 세션 유효성을 다시 확인합니다. 이미 실행 중인 엔진 호출을 강제로 중단하는 방식은 아닙니다.

- 액터 검색에 이름·16진수 RefID를 입력하고 Enter를 누르면 근거리 드롭다운의 거리·32명 제한 밖에 있는 기존 개별 액터도 찾습니다. 이름 확인 작업은 분할 처리하고 닫기·로드·새 검색 시 이전 검색을 취소합니다. NPC 생성·소환이나 셀 강제 로드는 하지 않습니다.
- 3D 미로드 액터도 호환성 검사를 통과한 바디·스킨·후타스킨·오버레이를 확정하여 선택값을 저장할 수 있습니다. 미리보기는 3D 로드가 필요하며, 저장한 선택은 기존 액터 로드 큐에서 호환성을 재확인한 뒤 적용합니다. 게임을 저장하면 미적용 선택도 기존 코세이브 형식으로 보존합니다. 아직 생성되지 않은 NPC 원본은 개별 RefID 선택 대신 배포 조건을 사용해야 합니다.
- 검증: Release 빌드와 회귀 테스트 28개 통과. 비정상 목록 항목·Editor ID 훅, 현재 제품 초기화 함수, 취소 경계, 스냅샷 메모리 수명, 게시/초기화 경합 400회, 미적용 선택 코세이브 왕복 검사를 포함합니다. 지원 SE/AE 12개 버전의 Address Library에서 필요한 주소 존재 여부를 확인했습니다. 제공된 1.2.6 충돌 경로는 보강했으며 제보 환경의 해결 여부와 원거리 NPC 선택·접근 적용은 인게임 확인이 필요합니다. 객체가 비정상 상태가 된 최초 원인은 확정하지 못했습니다. 기존 RaceMenu 계약·배포 규칙·코세이브 형식은 변경하지 않았습니다.
- 산적을 포함하여 적대·비적대 NPC 모두 액터 드롭다운에 표시할 수 있도록 적대 필터를 제거했습니다. 거리 4,096·가까운 32명 제한과 이름·NPC 판정·생존·활성·3D 로드 조건은 유지합니다. 배포 규칙 판정은 변경하지 않았습니다.

## 1.2.6

- Windows 마우스 메시지에 의존하지 않고 Skyrim 입력 훅에서 버튼·휠 입력을 받아 1.2.5의 마우스 전달 회귀를 수정했습니다. 게임 커서 좌표는 유지하며 Windows·Scaleform 중복 전달은 하지 않습니다.
- 렌더 프레임 사이의 짧은 우클릭도 보존하며, 누른 상태·중복 버튼 입력을 제외하고 포커스 이탈·닫기·재열기 시 대기 입력을 정리합니다.
- 반복 3D 알림에서 즉시 API로 확인한 얼굴의 속성 경로·실제 텍스처·렌더러 및 필요한 저장 오버라이드가 모두 맞으면 동일 DDS를 다시 로드하지 않습니다. 다른 스킨 전환, 손상 텍스처 복구, 미리보기 확정, 기본값 복원 및 별도 1인칭 얼굴의 적용 경로는 유지합니다.
- 프리셋 목록 최적화·저장 모프 복구·코세이브 형식·배포 선택·지원 런타임 표는 유지합니다. RSV·Selector 호환 패치나 Skyrim 1.7.x 지원은 추가하지 않습니다.
- 검증: Release 빌드와 자동 회귀 테스트 26개 통과. 60·15·5 FPS에서 Windows 메시지 없는 게임 입력 재생을 포함합니다. 제보자의 스킨 깜빡임은 재현하지 못했으며, 중복 재로드 수정이 모든 깜빡임 원인을 해결했다는 의미는 아닙니다.

## 1.2.5

- 마우스 입력 좌표를 게임 커서 기준으로 통일했습니다. Windows 버튼 입력은 렌더 단계까지 보관하여 OS 좌표와 Skyrim 커서 좌표가 섞이지 않도록 하고, Scaleform의 중복 마우스 전달을 제외합니다.
- 매 UI 프레임마다 전체 프리셋의 슬라이더 데이터를 복사하지 않고, 새로고침 시 만든 변경 불가 목록 메타데이터를 공유합니다. 화면 밖 바디프리셋 행은 생략하되 키보드 포커스·이동은 유지합니다.
- 액터 복원·3D 갱신 시 이전 세션 내 적용 성공 기록만 신뢰하지 않고 실제 바디 모프 레이어를 재확인합니다. 레이어가 없으면 기존 선택으로 복구하며 배포를 다시 추첨하거나 미리보기를 중단하지 않습니다.
- 일반 NPC 선택 조건은 제보 확인 전까지 변경하지 않았습니다. 입력·복구 변경의 인게임 검증은 아직 남아 있습니다.

## 1.2.4

- SetSlider가 없는 유효한 UBE Zeroed 프리셋을 미리보기·확정·저장된 선택 복원에서 허용합니다.
- 일반 UBE XML 끝값을 실제 Zeroed 빌드 기본값에 대한 차이로 적용합니다. 누락 끝점, 음수·100% 초과 값과 다른 바디 계열의 기존 계산은 유지합니다.
- 몸 재생성 완료 상태를 직접 확인하여 얼굴 갱신이 NiNode 알림에만 의존하지 않도록 보완했습니다. 실제 준비 상태, 오래된 요청 취소 및 제한된 실패 처리는 유지합니다.
- 몸·손·발의 Skin TXST가 없으면 고정된 UBE 경로 대신 원본 NIF의 실제 피부 재질에서 적용 대상을 만듭니다. 전체 채널 기본값과 노멀맵 방식을 보존하며, 서로 다른 아틀라스는 하나로 강제하지 않고 복제 모델의 형상별 대체 텍스처로 처리합니다. 기존 네이티브 텍스처 제공자는 우선 유지합니다.
- 처음 선택한 팩과 무관하게 재사용 가능한 몸 적용 대상을 준비합니다. 얼굴 전용 팩을 먼저 선택한 뒤 몸 포함 팩으로 바꿀 때 몸 적용 대상이 빠지던 문제를 수정했으며, 얼굴 전용 적용 자체는 몸에 연결하지 않습니다.
- 커스텀 NPC의 바디 계열 판정을 개선했습니다. 실제 피부 근거를 오래된 폼·폴더 이름보다 우선하고, 애드온 정보는 해당 종족에 맞는 몸 부품으로 제한하며, UBE 설치 여부만으로 확인된 일반 계열을 뒤집지 않습니다. 모든 커스텀 NPC를 한 계열로 강제하지 않고 CBBE/UNP 프리셋 누락의 코드상 원인을 수정했습니다.
- 기존 3D 갱신 이벤트에서 해당 액터의 계열 캐시를 무효화하고 일상적인 분류 로그를 줄였습니다. 팩 전환 시 생성된 스킨 그래프를 재사용하며 OBody/OClothe 키 정리, 모프 보존 옵션, 부분 DDS 적용, 미리보기 취소 및 코세이브 형식은 유지합니다.
- 영문·한글 안내를 갱신했습니다. OBody NG, Racial Skin Variance - SPID (RSV), RaceMenu Selector of Skins - Unique Player Character를 동시 외형 제어 비호환 모드로 안내합니다. 버튼을 ORefit 의상 보정 규칙 등록으로 바꾸고 ORefit JSON Master List 링크를 안내합니다.
- 임시 얼굴 미리보기 진단 기록 기능을 정식 소스와 빌드에서 제거했습니다. RSV·Selector 호환 훅이나 충돌을 자동 차단하는 패치는 포함하지 않습니다.
- 검증: 자동 테스트 실행 파일 25개, 액터 계열 근거 조합 4,374건, 실제 설치 UBE OSP 기반 재구성 계산 17,045건. AE 1.6.1179의 몸·얼굴 비대칭을 포함한 제보 환경의 인게임 재현 확인은 아직 남아 있습니다.

## 1.2.3

- 바디스킨 미리보기를 확정 적용 기록과 플레이어의 RaceMenu 저장 키에서 분리했습니다. NPC 배포 체크박스와 기본 스킨 미리보기도 포함하며 같은 팩의 미리보기와 확정을 구분합니다.
- 얼굴 채널과 몸·손·발 TXST의 부분 적용 실패 시 복원 처리를 추가했습니다. 얼굴 실패에 대비해 마지막 완료된 몸 상태를 보관하며, 다른 모드의 후속 소유권을 존중하고 빈 얼굴 텍스처 경로는 로드하지 않습니다.
- DDS 새로고침에 따른 내용 변경과 미리보기/확정을 작업 세대에서 구분합니다. 완료 콜백과 로드 경계의 복원 스냅샷을 정리하되 기존 네이티브 그래프 재사용은 유지합니다.
- 부분 DDS 적용, NPC 배포, 기존 세이브 형식과 1.2.1/1.2.2의 모프 수정은 유지합니다. 매 프레임 파일 검사·추가 타이머·대량 진단 로그는 넣지 않았습니다.
- 검증: 미리보기 중 저장·읽기와 채널 쓰기 실패 주입을 포함한 호스트 회귀 테스트 24개. 인게임 반복 전환·저장/로드 확인은 아직 남아 있습니다.

## 1.2.2

- Breasts/breasts, Waist/waist, HipBone/Hipbone처럼 대소문자만 다른 슬라이더의 충돌을 수정했습니다. XML의 small/big 값을 하나의 모프로 병합하며 처음 표기된 이름은 유지합니다.
- 미리보기와 가슴·유두 의상 보정에서 대소문자가 다른 이름을 잘못 합성하던 문제를 수정했습니다. 미리보기 취소 시 확정 상태로 복원하는 동작은 유지합니다.
- UNP 역방향 기본 슬라이더에도 같은 이름 비교를 적용했습니다. 음수·100% 초과 값을 유지하며 원본 XML과 코세이브 형식은 바꾸지 않았습니다.
- 모프 보존을 켠 상태의 OBody/OClothe 키 정리, 후타스킨 즐겨찾기, 성별별 NPC 배포 등 1.2.1의 기존 기능은 모두 유지합니다.
- 최초 공개 1.2.1 이후 추가했던 체크박스 미리보기 액터의 커스텀 팔로워·노인 자동 선택 제외도 포함합니다. 일반 액터 직접 선택과 실제 배포 규칙은 바꾸지 않습니다.

## 1.2.1

- NPC 배포 체크박스 모드에서 가까운 미리보기 액터를 자동 선택할 때 커스텀 팔로워·노인 NPC를 제외합니다. 후타에도 동일하게 적용하며 남는 대상이 없으면 플레이어를 선택합니다. 일반 액터 직접 선택과 실제 배포 규칙은 바꾸지 않습니다.
- NPC 배포 도구 행을 여성 · 남성 · 배포 하기 · 배포 취소로 변경했습니다. 여성 기본 선택, 성별별 가까운 NPC 자동 선택, 해당 NPC가 없을 때 플레이어 복귀를 지원합니다. 후타는 등록된 여성 후타 NPC만 자동 선택합니다. 배포 목록·새 규칙은 미리보기 액터와 별개인 배포 성별을 따르고, 바디·스킨은 설정된 배포 바디 타입도 반영합니다. 성별 전환 시 이전 체크·미리보기를 정리합니다.
- 바디 프리셋 확정과 기본 바디 복원 시 `OBody`·`OClothe` 키도 제거하여 OBody 체형·의상 보정이 BCNG와 중복되는 문제를 수정했습니다. ‘다른 모드의 모프 보존’이 켜져 있어도 두 키는 제거하며, 그 외 모프는 보존합니다.
- 미리보기는 OBody 제거 후의 결과를 보여주되 원본 키는 확정 전까지 유지합니다. 취소하면 원래 상태로 돌아가며, 이전 세이브의 BCNG 바디도 새 교체 정책으로 한 번 재평가합니다. 실행 중인 OBody의 자체 재배포를 비활성화하는 기능은 아닙니다.
- 후타스킨 탭에도 다른 목록과 동일한 별 버튼과 즐겨찾기 필터를 추가했습니다. 일반 목록과 NPC 배포 체크박스 목록 모두 지원합니다.
- 후타스킨 즐겨찾기는 설정에 저장되어 재실행 후 복원됩니다. ERF·TRX·UBE TRX 항목은 각각 구분하며 기존 즐겨찾기와 사용자 설정은 유지합니다.
- 별 클릭은 스킨 미리보기·적용이나 NPC 배포 체크를 바꾸지 않습니다. 긴 행의 글자는 별 영역을 침범하지 않게 생략합니다.

## 1.2.0 — v1.1.4 대비 최종 변경 내역

개발 중간의 수정 시도 순서가 아니라 최종 v1.2.0의 동작을 정리한 내역입니다. 바디프리셋·스킨·틴트 편집·후타스킨·NPC 바디/스킨 배포는 v1.1.4에도 있던 기능이며, 아래는 이를 변경하거나 확장한 내용입니다.

### 선택 방식과 UI
- 한 번 클릭/목록 이동은 미리보기, 더블클릭·Enter·게임에 설정된 Activate 입력은 적용으로 통일했습니다. 확정하지 않고 닫으면 마지막 미리보기를 적용하지 않고 기존 확정 선택을 복원합니다. 키보드·게임패드의 확정과 닫기는 게임의 Activate 및 메뉴 Cancel 설정을 따릅니다.
- 탭 순서를 바디프리셋 → 바디스킨 → 틴트마스크 → 조건부 후타스킨 → 오버레이로 정리했습니다.
- 마우스 오른쪽 드래그와 함께 LT + RS 좌우 캐릭터 회전을 지원합니다. 얼굴 카메라 위치, 입력 차단, 팝업 정렬, 긴 행 생략과 목록 높이 흔들림을 개선했습니다.
- 의상·랜덤화 팝업을 처음 열 때 화면 위로 붙던 문제를 수정하고, 지원 팝업마다 이동한 위치를 기억해 다음 열기에 복원합니다.
- 본창 타이틀바의 X 바로 왼쪽에 캐릭터 회전 안내를 복원했습니다. 표시 영역과 우측 정렬을 수정하고, 긴 문구는 숨기거나 자르지 않고 폭에 맞춰 축소합니다.
- 탭 안내 문구를 줄이고 남은 폭에 맞춰 한 줄로 표시합니다. 영문 오버레이 안내도 간결하게 정리했습니다.
- NPC 배포 진입 버튼을 지원 탭별 목록으로 옮기고 체크박스 선택·종류별 조건 팝업을 추가했습니다. 팝업에서 규칙을 추가할 때도 선택 후보가 유지됩니다.
- 바디프리셋·바디스킨·후타스킨 NPC 배포 목록에서도 체크박스 클릭과 키보드·게임패드 선택·이동으로 마지막 호환 항목을 미리보기합니다. 체크는 배포 후보 선택이며 액터의 수동 적용으로 저장하지 않습니다. 배포 취소·선택 해제·조건 팝업 진입·액터/탭 변경·창 닫기 때 해당 확정 외형을 복원합니다.
- 창을 열 때마다 무거운 목록을 다시 구성하지 않도록 갱신 작업과 스냅샷을 사용합니다. 성능 모드의 자동 작업 간격은 수동 선택과 분리됩니다.
- UI 안내와 전체 설치 경로를 정리했습니다. 설치된 스킨/후타스킨 모드 폴더를 통째로 BodySkin/Futanari 안에 복사하는 방식으로 안내합니다.
- 게임 실행 중 바디프리셋 XML과 BodySkin/Futanari 팩을 추가·교체한 뒤 복사 완료 → 새로고침 → 재선택·적용하는 방법을 안내했습니다. 새 플러그인·애드온 설치나 MO2 새 모드 활성화는 재실행이 필요하며, 오버레이 모드 설치에는 해당하지 않습니다.
- 프리셋 XML 값을 의도한 기준 바디에 표현하도록 캐릭터 바디와 의상을 Zeroed Sliders 프리셋 및 Build Morphs 체크 상태로 빌드하는 BodySlide 준비 과정을 추가했습니다.

### 바디프리셋과 모프 보존

- 모드 설정에 다른 모드의 모프 보존 옵션을 추가하고 기본값을 켬으로 설정했습니다. 캐릭터 기본 위치는 왼쪽, 게임 일시정지와 성능 모드는 해제이며 기존에 저장한 사용자 설정은 유지합니다.
- 프리셋 XML의 슬라이더 값을 액터 체중에 맞춰 보간하며 음수와 100% 초과 값도 유지합니다. 확정 프리셋 적용에서 전체 모프 합계를 상쇄하던 계산을 제거했습니다.
- 모프 보존을 켜면 BCNG 소유 키만 교체합니다. 끄면 프리셋 확정 시 다른 RaceMenu 바디 모프를 지우고 선택한 프리셋을 적용합니다. 어느 설정에서도 확정하지 않은 미리보기는 취소해 복원할 수 있습니다.
- 기본 바디와 액터 초기화는 현재·구형 BCNG 소유 키만 제거합니다. 모프 보존을 꺼도 다른 모드의 모프를 초기화로 지우지 않습니다.
- 자동 가슴·유두 보정은 다른 모드까지 합산한 모프가 아니라 OBody NG와 같은 프리셋 키 기준으로 계산합니다. 미리보기와 확정 적용도 같은 보정 정책을 공유합니다.
- 더 이상 쓰지 않는 전체 프리셋 슬라이더 합집합 캐시와 호환 보조 코드를 제거해 프리셋 전환 시 불필요한 목록 전체 작업을 없앴습니다.

### 스킨 적용과 에셋 처리
- 일반 BodySkin의 라이브 재질 덮어쓰기 경로를 몸·손·발용 전용 네이티브 Skin Armor/ARMA/TXST 경로로 바꿨습니다. 원래 제공자의 폼과 BCNG 복제본을 분리합니다.
- 얼굴은 별도 NiOverride 피부 채널 경로를 사용합니다. 확인된 구형·신형 RaceMenu 어댑터와 직접 접근 불가 시 대안을 사용하며, 최종 구현은 얼굴 HeadPart나 NIF를 교체하지 않습니다.
- 반복 선택 때 얼굴 지연·다른 팩 표시·보라색 얼굴을 일으키던 갱신 순서, 오래된 요청 취소, 기본값 복원과 텍스처 경로/캐시 처리를 정리했습니다.
- 대체된 일반 스킨 라이브 재질 코드와 이전 얼굴 실험 경로를 런타임에서 제거했습니다.
- 일부 파일만 있는 팩에서도 대상에 적용 가능한 DDS 채널을 반영합니다. BnP CBBE처럼 별도 성기·질·항문 대상이 없는 팩이 일반 피부 전체를 차단하지 않도록 했습니다.
- 인식되는 종족·노인·흡혈귀용 DDS → 선택 팩의 일반 채널 → 팩에 둘 다 없을 때 원래 제공자 순으로 처리합니다. 남성·여성에 같은 정책을 사용합니다.
- UBE skin/subsurface 채널 배정을 바로잡고 일반 계열 아틀라스 경로와 분리했습니다. 몸·손·발·얼굴·애드온 역할을 서로 임의 대체하지 않습니다.
- 한글 등 유니코드 경로, 모드 매니저 제공자 탐색, 텍스처 캐시 식별과 RSV를 포함한 원래 제공자 복원 처리를 개선했습니다.
- 스킨 profile.json 해석을 제거했습니다. 팩 식별과 지원 텍스처 경로를 기준으로 읽습니다.

### 후타스킨과 남성 성기 스킨
- 지원 SOS/TNG 남성·여성 애드온 텍스처를 네이티브 TXST 생성 경로로 변경하고, 이미 부착된 지원 애드온의 갱신도 연결했습니다.
- 읽을 수 있는 인접 메모리 영역에 엔진 import가 나뉘어 있을 때 정상 SOS/TNG 남성 성기·후타스킨 변경까지 차단하던 검증 오판을 수정했습니다. 코드 패턴·소유권·메모리 접근 검사는 유지합니다.
- 지원 여성 애드온의 설치·로드 여부로 후타 기능을 활성화합니다. 일반 남성 애드온만으로 활성화하지 않습니다.
- 현재 보이는 지오메트리만이 아니라 SOS/TNG의 지원 여성 애드온 등록을 기준으로 수동 변경 대상을 판단합니다. 미등록 여성과 남성은 대상이 아닙니다.
- 등록된 여성 후타 NPC로 제한된 후타스킨 전용 배포를 추가했습니다. 텍스처 선택이 후타 등록이나 장비 장착을 대신하지 않습니다.
- 대상 액터의 애드온 지오메트리가 일시적으로 사라져도 독립적인 후타스킨 선택을 유지합니다.

### 틴트마스크와 오버레이
- 틴트 검색을 옛 독립 TintMask 루트에서 각 BodySkin 팩의 Textures 구조로 옮겼습니다. 스킨과 틴트를 각각 읽으며 틴트 전용 팩과 스킨/틴트 혼합 팩을 지원합니다.
- 틴트 성별 및 UBE/일반 계열 필터링을 추가했습니다. 틴트 편집은 계속 플레이어 전용이며 NPC 배포는 없습니다.
- RaceMenu 페인트와 SlaveTats 컬렉션을 한 목록의 얼굴·몸·손·발 펼침/접힘 구조로 표시합니다.
- 부위별 수동 복수 오버레이와 더블클릭 적용/해제를 지원합니다. 부위 기본값은 BCNG 소유 오버레이만 제거합니다.
- 오버레이 즐겨찾기와 항목별 색상·투명도 조절을 추가하고, 조절부는 스크롤 목록 밖에 고정했습니다.
- 본체 UI를 열어 둔 동안 미리보기 색상·투명도를 항목별로 기억하고, 선택한 오버레이 후보마다 해당 RGBA를 NPC 배포 규칙에 복사합니다.
- NPC 배포 체크박스 목록은 체크한 오버레이를 부위별로 누적 미리보기하고 체크 해제 시 해당 항목만 제거합니다. 색상 편집 대상은 현재 행이며 체크된 후보의 색상·투명도 변경을 화면에서 확인할 수 있습니다. 전체 선택·선택 해제와 키보드·게임패드 확정 입력도 같은 체크 상태를 따릅니다. 슬롯 한도를 넘는 후보는 다른 모드의 슬롯을 덮어쓰지 않고 배포 후보로 유지합니다. 배포 취소·창 닫기 시 임시 스택만 복원하며 액터의 확정 선택·색상·적용 수는 바꾸지 않습니다.
- 이미 적용된 BCNG 오버레이의 미리보기는 소유 슬롯을 재사용하고 취소 시 확정 색상을 복원해 중복으로 겹치지 않도록 했습니다. 부위 기본값 행을 선택하면 바디스킨 탭과 같은 기본 카메라 구도로 돌아갑니다.
- 틴트 원본 DDS·색상 백업을 공용 설정에서 세이브별 TINT v2 기록으로 옮겼습니다. 확정 틴트 선택과 미리보기를 분리하고 팩·레이어 복원을 한 번의 합성 갱신으로 처리합니다.
- 반복 선택, 소유권, 복원과 제공자 키 수명 처리를 정리했습니다. 미리보기 슬롯은 확정 적용 수에 포함하지 않습니다.
- 부위 헤더는 적용 X/Y로 표시합니다. X는 BCNG 확정 수, Y는 RaceMenu 총수에서 외부 점유·예약 슬롯을 뺀 수입니다. 별도 BCNG 숫자는 삭제했습니다.

### 의상 보정과 SFS

- SFS Rendered Outfit API v1을 통한 가슴·유두 보정 선택 연동을 추가했습니다. ORefit 제외·강제 적용·의상별 전용 프리셋에도 같은 기준을 사용합니다.
- 보이는 실제 장비는 실제 장비의 이름·FormID·플러그인, 보이는 등록 외형은 해당 등록 외형의 정보로 판단합니다. 숨겨진 의상은 제외하고 보정 대상 의상이 모두 숨겨지면 가슴·유두 보정을 해제합니다.
- SFS 표시 변경 알림으로 해당 액터만 재평가하며 오래된 대기 보정을 폐기합니다. SFS나 지원 API가 없거나 SFS 관리 대상이 아닌 액터는 기존 실제 착용 장비 기준을 유지합니다.
- SFS의 게임 작업용 조회 API를 연결해 작업 스레드가 바뀐 뒤 실시간 가슴·유두 보정 갱신이 멈추던 문제를 수정했습니다. 기존 API 대체 경로와 오래된 결과 검사는 유지합니다.

### NPC 조건과 저장
- 새 설치의 규칙은 비어 있는 선택형 배포로 제공합니다. 배포/배포 제외 모드를 없애고 schema 7 변환에서 옛 제외 전용 항목을 제거하며 적합한 양의 배포 규칙은 유지합니다.
- 전체 NPC 조건의 커스텀 팔로워 제외·노인 NPC 제외는 기본 체크된 대상 필터입니다.
- 커스텀 팔로워·노인 NPC 제외 체크는 전체 NPC 조건에만 적용합니다. 이름·개별 NPC 등 다른 대상 조건을 숨은 필터로 차단하던 문제를 수정했습니다.
- 후타 등록이나 애드온 계열이 뒤늦게 바뀌면 후타스킨 배포 후보를 다시 평가합니다. 바디·스킨·오버레이를 다시 추첨하거나 수동 선택을 덮어쓰지 않으며, 애드온 지오메트리뿐 아니라 선택 팩과 텍스처 내용도 추적해 새 후보를 실제로 재적용합니다.
- OBody NPC 조건 가져오기를 제거했습니다. OBody ORefit JSON 읽기는 명시적인 의상 보정 규칙 등록에만 남깁니다.
- v1.1.4의 닫을 때 자동 저장을 변경했습니다. 모든 배포 지원 탭에서 닫기·X·Esc·본체 창 닫기는 저장하지 않은 조건 편집을 취소합니다.
- 로드된 NPC 즉시 배포 또는 다음 게임 실행 시 배포로만 저장합니다. 즉시 배포는 저장 성공 후 활성화하며, 다음 실행용 저장은 현재 활성 규칙을 바꾸지 않습니다.
- 규칙 우선순위는 기능별·오버레이 부위별로 평가합니다. 자동 오버레이는 설정된 부위당 후보 하나를 고르고, 수동 선택은 복수 스택을 지원합니다.
- 확정 수동 선택, 복수 오버레이/색상, 기본값과 애드온 선택의 세이브 처리를 확장했습니다. 수동 선택은 자동 배포보다 우선합니다.
- 선택 액터 설정 값 초기화 / 전체 액터 설정 값 초기화를 바디·바디스킨과 지원 남성 성기·후타스킨·오버레이·플레이어 틴트 전체로 확장했습니다. 기본값 의도를 저장하며 에셋이나 규칙 파일은 삭제하지 않습니다.

### 호환과 검증
- 명시적 대상: SE 1.5.97; AE 1.6.317, 1.6.318, 1.6.323, 1.6.342, 1.6.353, 1.6.629, 1.6.640, 1.6.659, 1.6.1130, 1.6.1170, 1.6.1179. VR·Epic 1.6.678·목록 밖 버전은 지원하지 않습니다.
- 확인된 RaceMenu BodyMorph·Override/Overlay 세대와 구형 ABI, 제공자의 실제 슬롯 수를 점검했습니다. 게임에 맞는 RaceMenu/SKSE는 여전히 필요합니다.
- 성기 백엔드의 두 버전 고정 주소 제한을 버전별 Address Library 조회와 코드·소유권 검증으로 바꾸고 GOG의 import 조회 가정을 수정했습니다.
- 중복 탐색·임시 진단·폐기 코드를 줄이되 필요한 실패 보고와 소유권·수명 검사를 유지했습니다.
- SE/AE 전용 Release 빌드와 자동 회귀 테스트를 통과했습니다. 모프 키 보존·미리보기 복원·오버레이 선택·SFS 연동·성기 검증 경로를 포함합니다.

### 업데이트 주의
- .ess와 같은 이름의 .skse 코세이브, 설정, 사용자 배포 JSON, 에셋을 백업하세요.
- 사용자의 BodyChangeNGdistribution.json을 빈 설치 시작 파일로 교체하지 마세요.
- 스킨 모드 폴더는 Data\BodySkin\, 후타스킨 모드 폴더는 Data\Futanari\ 아래에 통째로 두고 원래 Textures 구조를 보존하세요.
- 옛 틴트는 Data\BodySkin\팩 이름\Textures\actors\character\character assets\tintmasks\로 옮기고, 변환된 배포 조건을 확인한 뒤 저장하세요.

## 1.1.4 — 2026-09-06

### NPC 스킨 적용 순서와 유지

- 구형 v0/v1 Papyrus 스킨 변경이 대기 중일 때 바디 목록을 빠르게 연속 선택해도 오래된 UI 바디·스킨 작업을 폐기합니다. 마지막 바디를 한 번 적용한 뒤 선택한 바디스킨과 후타나리 스킨을 한 번만 다시 적용하며 자동 배포·장비 갱신 작업은 유지합니다.
- 자동 배포되는 NPC 스킨은 해당 액터의 지연 바디·의상 재구성이 끝난 뒤 적용합니다. NPC 스킨이 잠깐 보였다가 즉시 원본으로 돌아가던 문제를 수정했습니다.
- `QueueNiNodeUpdate` 완료 뒤 최종 Biped 클론을 다시 칠하기 전에는 스킨 적용을 완료로 기록하지 않습니다. 최종 재적용은 RaceMenu Override v0/v1과 v2에서 동일한 몸·손·발·얼굴·성기·항문 정확 부위 경로를 사용합니다.
- NPC의 스킨 또는 기본 스킨 미리보기가 접수되는 즉시 수동 선택으로 고정하여 액터 부착·초기화 이벤트의 자동 배포가 현재 선택을 덮어쓰지 못하게 했습니다.

### 사망 NPC 배포와 규칙 편집

- 이미 죽어 있지만 3D가 로드된 NPC도 자동 배포에 포함합니다. 플레이어·비활성 액터·3D 미로드·비 NPC 제외 조건은 유지하며 적용 서명과 액터 작업 병합으로 중복 작업을 막습니다.
- NPC 배포 팝업을 X나 Escape로 닫거나 메인 메뉴를 닫을 때 현재 편집 초안을 저장합니다. 다음 실행용 JSON만 갱신하며 현재 세션에 활성화된 규칙은 묵시적으로 바꾸지 않습니다.
- 같은 세션에서 팝업을 다시 열면 편집 초안을 유지하지만 세이브 로드 경계에서는 초기화하여 이전 세이브의 미완성 편집값이 새 세이브로 누수되지 않게 했습니다.
- 바디·스킨 풀에 한국어·영어·중국어 `전체 선택`과 `전체 해제`를 추가했습니다. 규칙의 성별과 모드 설정의 NPC 바디 계열에 맞는 항목만 선택하며 Zeroed Sliders는 선택 후 사용자가 해제할 수 있습니다.

### 성능과 검증

- 상시 검사, 카탈로그 재검색, 프레임별 JSON 저장을 추가하지 않았습니다. 바디와 스킨이 함께 바뀌는 액터만 한 번 지연 평가하고 실제 Biped 재구성이 발생한 경우에만 최종 재적용을 한 번 수행합니다.
- Release 빌드와 회귀 테스트 실행 파일 12종을 모두 통과했습니다. 기존 설정·배포 스키마·사용자 규칙·코세이브 식별자·바디모프 소유권·부분 스킨팩·UBE 경로·후타나리 분리를 유지합니다.

## 1.1.3 — 2026-09-06

### CBBE 3BA 손·발 BodySkin 적용

- RaceMenu의 넓은 스킨 슬롯 호출이 같은 Skin Armor의 모든 ArmorAddon을 순회하면서, 마지막 발 적용값이 손까지 다시 덮던 원인을 수정했습니다.
- CBBE 3BA·BHUNP·바닐라 계열은 몸·손·발의 정확한 ArmorAddon과 로드된 지오메트리에만 각 부위 텍스처를 적용합니다. CBBE 계열 발은 의도대로 몸 아틀라스를 사용하면서 손은 전용 손 텍스처를 유지합니다.
- SFS 등으로 화면에서 숨긴 장비가 실제 Biped 슬롯을 계속 점유하면, 현재 없는 일반 계열 부위에만 1비트 영구 키를 저장합니다. Override v0/v1은 구형 광역 콜백 뒤 현재 보이는 정확한 각 부위를 다시 복구하고, v2는 화면을 광역 재도색하지 않고 누락 키만 저장합니다.
- 플레이어/NPC 수동 선택, NPC 직접 지정, 조건 배포, 의상 착탈·사망 NPC 약탈 뒤 재적용이 모두 같은 수정 경로를 사용합니다.
- 기존 세이브에서 적용 완료로 기록된 NPC도 새 세션의 실제 몸·손·발 상태가 다르면 한 번 다시 적용합니다. 상시 파일 검색이나 전체 NPC 반복 검사는 추가하지 않았습니다.
- UBE의 몸·손·발 공용 아틀라스 경로는 유지하며, 일반 계열의 불필요한 광역 적용 호출만 제거했습니다.
- 구형 SE Override v0, UBE의 AE 역포트 Override v0, 정식 Override v1, 정식 Override v2를 별도 경로로 판정합니다. 두 v0과 v1은 직렬화에 안전한 Papyrus 문자열 경로를, 검증된 v2만 native 인터페이스를 사용하며 미확인 신형 버전은 실패 폐쇄합니다.

### 후타나리·바디스킨 소유권 분리

- BCNG 자체 NiNode 재생성 중 지오메트리 클론이 잠시 분리되어도 장착 해제로 오인하지 않습니다. 액터의 Biped 슬롯에 남아 있는 ArmorAddon을 기준으로 후타나리 타입을 유지해 탭이 사라지지 않게 했습니다.
- 실제 TRX 노드명 `CBBE_Schlong`을 기존 표기 변형과 함께 인식해 UBE/CBBE TRX 대상에 한 번의 선택으로 적용합니다.
- 일반 바디스킨의 몸 적용·정리에서는 확인된 외부 후타나리 생식기 노드만 제외합니다. 일반 CBBE 3BA·BHUNP·UBE 몸/손/발, 내부 성기·항문 아틀라스, 남성 HIMBO·SAM 및 SOS 경로는 각각 기존 소유권을 유지합니다.
- 바디프리셋·BodyMorph 경로는 변경하지 않았습니다. 미리보기·확정·NPC 배포·기본 바디·의상 보정의 절대 프리셋 동작을 그대로 유지합니다.

### 검증

Release 빌드와 회귀 테스트 실행 파일 12종을 모두 통과했습니다. 기존 배포 규칙·설정·JSON·코세이브·부분 스킨팩·기본 스킨 복원·UBE 경로·일반 장비 갱신은 유지합니다.

## 1.1.2 — 2026-09-05

### 인터페이스 테마

- 네이티브 UI의 창·하위 패널·팝업·제목 표시줄·평상시 컨트롤·입력칸·카드·표·스크롤바·크기 조절 손잡이·탐색 영역을 검정·차콜·회색 바탕으로 통일했습니다.
- 선택·마우스 오버·눌림 상태의 기존 청색과 경고·성공·진행 상태·즐겨찾기·비호환 표시·실제 틴트 미리보기 색은 기능적 강조색으로 유지합니다.
- 주변 액터 목록 갱신 버튼을 한국어 `액터 새로고침`, 영어 `Refresh actors`, 중국어 `刷新角色`로 줄였습니다. 실제 로드 액터 갱신 동작은 그대로입니다.
- UI 배치와 스케일링, 키보드/게임패드, 카메라 표시, 카탈로그, 배포 규칙, 설정, JSON·코세이브 호환, 바디·스킨·틴트·의상 보정 경로는 그대로 유지합니다.

### CBBE 3BA 해부학 기능 안전 경계

- 의상 착용 시 가슴·유두 보정은 확인된 CBBE 3BA 액터에만 적용합니다. UBE 또는 계열이 불확실한 여성 액터에는 추정한 3BA/UBE 슬라이더 값을 기록하지 않습니다.
- 유두·생식기 형태 무작위화는 CBBE 3BA NPC 전용으로 제한하고, 한국어·영어·중국어 UI 명칭 앞에 `NPC`를 표시합니다.
- UBE 액터를 선택하면 미지원 안내와 각 옵션 툴팁을 표시합니다. 혼합 환경에서 UBE 플레이어는 건너뛰면서 CBBE 3BA NPC에는 설정을 사용할 수 있도록 전역 체크박스 자체는 유지합니다.
- 이전 빌드가 만든 의상 보정 소유 레이어는 미지원 계열을 만났을 때 한 번 제거하고, 이후 동일 의상 이벤트는 무작업 결과를 캐시해 반복 비용을 만들지 않습니다.

### 카탈로그 안내와 UBE BodySkin 연결

- UBE 몸 슬롯 53에 유효한 대상이 전혀 없을 때만 일반 몸 슬롯 32를 사용합니다. 얼굴만 바뀌고 몸이 바뀌지 않던 UBE 프로필을 처리하면서 하나의 몸 스킨을 두 지오메트리 경로에 중복 적용하지 않습니다.
- 메인 탭 이름을 `바디프리셋`, `바디스킨`, `틴트마스크`로 통일했습니다.
- 각 카탈로그의 빈 목록에 정확한 배치 경로와 새로고침 방법을 한국어·영어·중국어로 안내합니다.
- UI·동봉 TintMask 안내문·넥서스 설명에 틴트마스크 방식만 호환되며 오버레이 방식은 호환되지 않는다고 명시했습니다.
- 동봉 기본 규칙 8개와 런타임 재생성 기본 이름을 영어로 변경했습니다. 저장된 사용자 규칙 이름은 다시 쓰지 않습니다.

### 검증

Release 빌드와 회귀 테스트 실행 파일 12종을 모두 통과했습니다. 테마와 계열 정책 검사는 프레임별 검색·파일 작업을 추가하지 않으며, UBE 미지원 해부학 경로는 모프 적용 전에 종료합니다.

## 1.1.1 — 2026-09-05

### NPC 배포와 저장 복원

- 세이브 로드 후 수동 지정하거나 조건 배포한 NPC의 실제 바디·스킨을 한 번 확인합니다. 적용값이 없으면 다시 적용하고 이미 정상이면 건너뜁니다.
- 겹치는 액터 초기화·셀 부착·의상 작업을 합치고, 이전 세션 작업을 로드 시 무효화하며, 3D 미준비 재시도에 제한을 둡니다.
- 실제 적용에 성공한 결과만 완료 상태로 저장합니다.

### 바디 프리셋과 의상 보정

- RaceMenu 3BA MORPHS 값이나 반복 배포 위에 프리셋이 누적되지 않게 했습니다. XML에서 빠진 호환 슬라이더는 0을 목표로 하며 다른 모드의 모프 키는 유지합니다.
- 미리보기·확정·NPC 배포가 같은 절대 결과 적용 경로를 사용하고 오래된 작업이 현재 선택을 덮지 않게 했습니다.
- 바디 변경 뒤 전체 모프 결과를 기준으로 의상 보정을 다시 계산합니다.
- 액터의 실제 계열에 따라 CBBE 3BA와 UBE 2.0의 가슴·유두 보정 및 유두·생식기 랜덤화를 분리합니다.

### BodySkin 적용과 호환성

- 여러 부위가 한 Skin Armor에 있어도 몸·손·발 geometry를 분리하며 몸 텍스처가 손이나 발에 복제되지 않게 했습니다.
- 부분 스킨팩은 제공된 부위와 diffuse·normal·subsurface·specular 채널만 교체하고 없는 내용은 현재 텍스처를 유지합니다.
- 의상 재구성 및 사망 NPC 장비 약탈 뒤 선택 스킨을 복구하고, 사라진 BCNG 캐시 파일은 원본 스킨팩에서 다시 준비합니다.
- 액터별 CBBE 3BA·UBE 2.0, 일반 여성·남성 스킨, SOS 남성 생식기, 아르고니안·카짓, 선택적 노인·종족 얼굴 변형을 지원합니다.
- Racial Skin Variance 및 Mu Dynamic NormalMap 동반 파일 호환 처리를 추가했습니다.
- 액터 갱신과 비동기 작업 경계를 정리해 OverlayFix 동시 사용 호환성을 높였습니다.

### 반응성·성능·안정성

- 프레임 처리 예산 안에서 바디·스킨·틴트 직접 선택을 우선하면서 자동 배포도 공정하게 처리합니다.
- 선택 액터에게 필요한 스킨 파일만 단일 백그라운드 작업자에서 준비하고 최신 요청과 준비된 캐시를 재사용합니다.
- MO2가 같은 DDS를 실제 경로와 가상 Data 경로로 노출할 때 발생하던 전체 해시 중복을 제거하고, 대용량 파일별 로그는 디버그로 제한했습니다.
- DDS 전체 내용 읽기는 초기 카탈로그 로드와 명시적 새로고침으로 제한하며 같은 ID의 XML/DDS 변경을 감지합니다.
- 완료된 콜백 소유권을 즉시 해제하고, 돌아오지 않는 RaceMenu SE 콜백 묶음은 제한 시간 뒤 취소하며, 늦거나 오래된 후속 작업은 무효화합니다.
- BodySkin과 의상 보정을 사용하지 않는 액터의 장비 이벤트 작업을 생략하고, 빈번한 경로의 설정·규칙 복사를 줄였습니다.

### 검증

Release 빌드와 회귀 테스트 12종을 모두 통과했습니다. 액터 상태 복원, 작업·콜백 수명, 바디 계열 분리, 부분 스킨 연결, 카탈로그 새로고침, MO2 경로 별칭 중복 방지를 자동 검증했으며 기존 규칙·JSON·설정·코세이브 식별자·기본 제외 샘플·즐겨찾기·카메라 값은 유지합니다.

## 1.1.0 — 2026-09-04

### 바디 프리셋과 혼합 바디 환경

- 호환 바디 계열의 슬라이더 집합을 기준으로 프리셋의 절대 목표 체형을 적용합니다. XML에서 생략한 슬라이더는 0을 목표로 하며, 미리보기·확정·NPC 반복 배포가 기존 체형 위에 같은 값을 계속 더하지 않도록 했습니다.
- 전체 모프 삭제 대신 BCNG 소유 보정값을 사용합니다. 다른 모드의 모프 키를 삭제하지 않으며 미리보기와 확정·NPC 배포의 소유권을 분리합니다.
- 로드된 스킨·얼굴 근거로 액터별 CBBE 3BA와 UBE를 감지합니다. XML의 Preset set·Group 정보를 우선 분류하고, 불확실하거나 여러 계열에 해당하는 프리셋은 안전한 표시 폴백을 유지합니다.
- 사용자가 선택한 배포 풀은 그대로 보존하고, 실제 바디·스킨 선택 시 액터 성별과 확인된 계열을 검사합니다. 의상 관련 이름이 있다는 이유만으로 바디 목록에서 일괄 제외하지 않습니다.

### 스킨 지원과 부위·채널 연결

- 남성 스킨 경로와 손·발 대상 탐색을 수정했습니다. 첫 번째 장비 슬롯에서 파트를 찾지 못하는 다중 슬롯 스킨 아머도 제한된 보조 탐색으로 처리합니다.
- 미리보기·확정·NPC 배포 모두 부분 스킨팩을 지원합니다. 몸·손·발·얼굴 및 Diffuse·Normal·Subsurface·Specular 중 제공된 부위·채널만 바꾸고, 없는 값은 원본을 유지하며 다른 부위나 채널로 대신 채우지 않습니다.
- UBE 2.0 Body·Head 아틀라스와 아르고니안·카짓 남녀 스킨을 액터별로 구분하며, 해당 몸 아틀라스를 사용하는 꼬리 지오메트리도 처리합니다.
- femaleold와 인간형 종족별 얼굴 노멀은 맞는 액터에게 파일이 있는 채널만 적용합니다. Astrid·Afflicted 전용 텍스처와 BodySkin 안의 틴트마스크 DDS는 바디스킨 적용 대상에서 제외합니다.
- CBBE 3BA femalebody_etc_v2_1은 질·항문 공용 아틀라스에, BHUNP/UNP BakaUNP/VaginalAnalCanal2는 해당 질·항문·내부 지오메트리에 연결하며 일반 바디 텍스처와 분리합니다.
- 스킨팩의 원본 SOS 폴더에서 Smurf Average·VectorPlexus Regular·VectorPlexus Muscular 슬롯 52 텍스처를 지원합니다. 현재 애드온·종족·노인 재질에 맞춰 적용하며 Muscular는 원본 구조에 맞는 Regular 채널 상속을 사용합니다.
- 확인된 UBE·일반 플레이어 틴트팩을 구분하되 계열 판정이 불확실할 때는 안전한 폴백을 유지합니다. 틴트 기능은 플레이어 전용입니다.

### NPC 배포와 성능

- 기존 조건에 키워드·클래스·전투 스타일을 추가했습니다. 팩션 드롭다운은 이름이 없는 레코드도 EditorID·플러그인·로컬 ID로 표시합니다.
- schema 4에서 팩션·종족·키워드·클래스·전투 스타일 대상을 플러그인+로컬 FormID로 저장합니다. 기존 schema 3 규칙도 읽을 수 있으며 저장 시 새 형식으로 전환합니다.
- 기본 제외 샘플 8개를 유지합니다. 커스텀 팔로워·노인 남녀는 바디만, 아르고니안·카짓 남녀는 스킨만 배포 제외입니다.
- 일반 모드와 성능 모드 모두 자동 액터·장비 이벤트를 병합하고 RaceMenu 파티션 갱신을 지연 처리하며 불필요한 의상 모프 재빌드를 건너뜁니다. 성능 모드는 추가 처리 간격을 둡니다.
- 최초 로드된 NPC 처리는 직렬화·RaceMenu·오버레이 로드 콜백이 몰리는 구간에서 두 게임 태스크 뒤로 옮겼습니다. 반복 파일 스캔이나 타이머를 추가하지 않으며 카탈로그 기반 슬라이더 집합은 캐시합니다.

### 마이그레이션·UI·릴리스

- 구형 플러그인 제거 후 BodyChange.esp 얼굴 HeadPart를 참조하는 일반 RaceMenu .jslot 파일을 복구합니다. 같은 위치에 백업을 만들고 사용 가능한 High Poly Head를 우선, 없으면 바닐라 대상을 사용하며 UBE 커스텀 헤드 프리셋은 보존합니다.
- 메인 탭 명칭을 바디프리셋·바디스킨·틴트마스크로 정리하고 한국어·영어 릴리스 문서를 갱신했습니다.
- 기존 설정·코세이브 식별자·소유 키와 BodyChangeNGdistribution.json 파일명을 유지합니다. 1.1.0 버전 변경만으로 이름을 바꾸거나 초기화하지 않습니다.

검증 범위: Release 빌드와 회귀 테스트 실행 파일 10종. 파일·NIF 근거와 자동 테스트로 연결·상태 로직을 검증했으며, 인게임 아틀라스 외형·충돌체 동작·OverlayFix 크래시 호환·무스터터 동작까지 보증하는 것은 아닙니다.

## 1.0.0 — 2026-09-02

첫 공개 안정 버전. 인게임 바디·스킨·플레이어 틴트, NPC 조건 배포, 코세이브 결과 저장, 의상 보정과 키보드·게임패드 UI를 제공합니다.

[상세 이전 이력](docs/UPDATE-HISTORY-v1.0.0-KO.md)
